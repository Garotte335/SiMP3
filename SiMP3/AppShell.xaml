<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SiMP3.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="SiMP3">
    <Shell.Resources>
        <Color x:Key="ShellBackground">#0A0A0A</Color>
        <Color x:Key="ShellAccent">#C084FC</Color>
        <Color x:Key="ShellSurface">#121216</Color>
        <Color x:Key="ShellBorder">#1F1F28</Color>
    </Shell.Resources>

    <Shell.TitleView>
        <Grid Padding="14,10" BackgroundColor="{StaticResource ShellBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Логотип/назва -->
            <Label
            Text="SiMP3"
            FontSize="21"
            FontAttributes="Bold"
            TextColor="White"
            VerticalOptions="Center"
            Margin="0,0,10,0"/>

            <!-- 🔥 SearchBar замість кнопки -->
            <Border
            Grid.Column="1"
            HeightRequest="40"
            BackgroundColor="{StaticResource ShellSurface}"
            Stroke="{StaticResource ShellBorder}"
            StrokeThickness="1"
            StrokeShape="RoundRectangle 14"
            Padding="4,2"
            Margin="0,0,12,0"
            HorizontalOptions="Fill"
            VerticalOptions="Center">

                <SearchBar
                x:Name="GlobalSearchBar"
                Placeholder="Пошук треків"
                TextChanged="OnSearchTextChanged"
                BackgroundColor="Transparent"
                FontSize="14"
                TextColor="White"
                PlaceholderColor="#A0A0A0"
                CancelButtonColor="{StaticResource ShellAccent}"
                HeightRequest="30"
                HorizontalOptions="Fill"
                VerticalOptions="Center"
                Margin="0" />
            </Border>

            <!-- Кнопка Settings -->
            <Border
            Grid.Column="2"
            WidthRequest="40"
            HeightRequest="40"
            BackgroundColor="{StaticResource ShellSurface}"
            Stroke="{StaticResource ShellBorder}"
            StrokeThickness="1"
            StrokeShape="RoundRectangle 12"
            HorizontalOptions="End"
            VerticalOptions="Center">
                <ImageButton
                x:Name="SettingsButton"
                Padding="10"
                BackgroundColor="Transparent"
                Source="menu.png"
                Clicked="OnSettingsClicked" />
            </Border>
        </Grid>
    </Shell.TitleView>

</Shell>
